<%= link_to("<< Back To List", {:action => 'index'}, :class => 'back-link') %>
<div class="create">
	<h2>Delete Item</h2>

	<%= form_tag({:action => 'destroy', :id => @item.id}) do %> <!-- this div contain button to delete and the name of item -->
	<p class="alert">Are you sure you want to delete this Item?</p>
	<p class=> <%= @item.item_name%> </p>
	<div>
		<%= submit_tag("Delete") %>
	</div>
	<% end %>
</div>